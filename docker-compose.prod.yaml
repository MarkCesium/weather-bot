services:
  redis:
    profiles: [disabled]

  bot:
    restart: unless-stopped
    networks:
      - weather-bot
      - shared-network

networks:
  weather-bot:
    driver: bridge
  shared-network:
    external: true
    name: shared-network
